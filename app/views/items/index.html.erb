<div class="row">
			<div class="col-12">

				<div class="card">
                    <h3>Email Utilisateur</h3>
                        <% if user_signed_in? %>
		                <p><%= current_user.email %></p>
                        <%else%>
                        <%end%> 
					<div class="table-responsive">
						<table class="table table-striped table-hover">
							<thead>
								<tr>
									<th>Prix de départ</th>
									<th>Pourcentage de remise</th>
									<th>Prix</th>
									<th></th>
								</tr>
							</thead>
							<tbody>
								<% @items.each do |item| %>
									<%= render partial: "items/table_row", locals: {item: item} %>
								<% end %>
							</tbody>
						</table>
					</div>
				</div>

			</div>
		</div>

	</div>